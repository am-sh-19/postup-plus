# postup+

Hackathon project for post-operative patient care.

## Run locally

From the repo root:

```bash
bun install
bun run dev
```

Or from the app folder:

```bash
cd web
bun install
bun run dev
```

Open http://localhost:3000
